RADI UNUTAR SOLUTION EXPLORERA! -Back
RADI UNUTAR DATEAPP! - Front
Od angular 16 verzije, nemamo vise app module, vec sve komponente kreiramo kao standalone
Ako se niko ne subscribuje na observable (tok podataka), nista se nece desiti


komande za kreiranje .NET apk:
dotnet new sln  	--pravimo novi solution
dotnet new webapi -controllers - API      --API je ime
dotnet sln add API 	--dodajemo u solution 

Koristimo SQLite kao bazu i ekstenziju u VS code (ne koristi se za produkciju ali je odlican za programiranje)
Ne treba nam database server, vec koristi fajl da smesti nasu bazu, ali kasnije cemo videti kako da to postavimo za produkciju sa pravom bazom

appsettings.Development.json
"ConnectionStrings": {
    "DefaultConnection": "Data source=dating.db"
  }
Program.cs
    opt.UseSqlite(builder.Configuration.GetConnectionString("DefaultConnection"));

Koristimo SQLite jer je cross-platform a SQL Server nije

Ubacivanje entity framework-a:
dotnet tool install --global dotnet-ef --version 8.0.12

Komande za migraciju i kreiranje baze
dotnet ef migrations add Ime
dotnet ef database update

Nakon toga u Search idemo >Sqlite Open DataBase , pa prvo sto izadje, 
nakon toga u explorer-u ce se pojaviti SQLITE EXPLORER i tu su nam je baza

Koristimo mkcert, da bi napravili sertifikat da moze nasa aplikacija da radi i na https adresi, a i da bi izbegli upozorenja da nasa stranica nije bezbedna, ovaj sertifikat obezbedjuje da podaci od klijenta do servera stignu bezbedno 

Koncept inherencije (ne ponavljanje koda) -> to resavamo tako sto pravimo Base API kontroler

Ako hocemo da testiramo imamo sa leve strane debugger, napravimo fajl koji trazi i dodamo prve 2 konfiguracije u tom fajlu,
Zatim idemo na RUN AND SET DEBUG => .NET Core Attach

Kako funkcionise JWT:
1.Korisnik salje serveru username i pass
2.server validira podatke i vraca klijentu JWT token, koji klijent cuva lokalno 
3.clijent salje jwt serveru jwt token sa svakim pojendinacnim zahtevom
4. server proverava da li je jwt token validan i vraca korisniku odgovor
Zakljucak: svaki put kad hocemo da pristupimo necemu sto je zasticeno atorizacijom, saljemo JWT token sa tim zahtevom, i server proverava da li je token validan i vraca odgovor
Token je oznacen sa secret key, on se nalazi u appsettings,
mora da se oznaci symetric ili asymetric key
Token je iznace i sa claim (nesto sto korisnik tvrdi o sebi (moje ime je to i to, moj rodjendan je tad i tad...))

Servisi u #C - kreiramo interfejs koji predstavlja apstrakciju toga sta ce servis raditi, bez definisanja kako to radi, i nas servis implementira taj interfejs sa definisanjem toga sta radi metoda iz interfejsa

Kada se korisnik registruje i loguje ne treba da mu vracamo passwordsalt passwordHash i username, vec samo username i token
Kada injectujemo u kontroler, injectujemo interfejs a ne sam servis

Sada hocu da testiram da li radi autentifikacija (section 4 u postman, get user with bearer token),
Login ujemo se kao bob npr, i onda posaljemo u header Bearer <token> na putanji https://localhost:5001/api/users/1 i vratice nam boba, sto znaci da je authentifikovan (prijavljen i moze da pristupi toj metodi, a metodu u kontroleru getUser:id smo inace oznacili kao [Authorisation], sto znaci da samo authentifikovan korisnik moze da pristupi )

Ne zelimo da nam konfiguracija cela jwtTokena i ostalih stvari bude implementirana u program.cs
Svi ovi servisi u program.cs su ekstenzije, pa pravimo folder extensions u API i kazemo da je klasa static, 
static znaci da mozemo da koristimo metode te klase bez kreiranja novih instanci !
Cilj je da prosirimo IServiceCollection u kojoj se nalaze inace sve one metode: addCors, addScoped, addAuthentication..

Signali u angularu mogu sadrzati bilo koju vrednost, od jednostavnih tipova do slozenih struktura,
signali su ustvari geteri koji obavestavaju komponenete kada dodje do promene vrednsoti 
Imamo 2 metode nad signalima , set i update.
Koristi se za sinhrone stvari, dok se observables koriste za asinhrone stvari 

Koristimo angular signal, da smestimo nas korisnicki objekat
Da bi smo resili problem osvezavanja stranice (da se ne izloguje korisnik kada se refreshuje), koristimo signale.
Kad se korisnik prijavi, cuva se u local storage, kada se odjavi brise se iz local storage

Imamo Home komponentu koja je parent i register komponentu koja je child, komunikacija se vrsi tako sto roditelj koji salje, ima Output dekorator, a register koja prima ima input, saljemo usere koje smo dobili iz baze u register komponentu
Roditelj koji salje, ima uglaste zagrade i properti iz child-a
Kad kliknemo na cancel, register (child) komponenta treba da posalje parent komponenti rezultat, da se forma ugasi 

Rute definisemo u Route komponenti (koju je angular definisao) i gde koristimo u toj komponenti ide: routerLink=""

Toastr sluzi da prikazemo greske korisniku

Moramo zastitit rute, jer npr samo registrovan korisnik moze videti nav bar itd, ali nepoznati korisnik moze ukucati https://localhost/members i da vidi sta je tu,
napravimo guard i u routes imamo canActivate

Welcome {{accountService.currentUser()?.username | titlecase}} - inicijalno vraca malo slovo ako je tako bilo u bazi, ovo ga vraca u veliko

Ima dela gde se pravi kontroler sa greskama i izuzecima ali nisam to radio 

Imamo 2 tabele , slike i usere, a imamo samo DBSet za usere, ne treba dam i DBSet za slike, jer su useri povezani sa slikama

Koristimo json generator, da bi napunili bazu podacima, kreiramo Seed.cs, to je klasa za popunjavanje baze pdataka

U program cs. imamo kod za inicijalizaciju base podataka prilikom pokretanja aplikacije

(Nije neophodno ali sam radio) : dodao sam sloj apstrakcije(Reposotory), napravio interfejs, implementirao ga, dodao u AplicationServiceExtensions
Izmenimo UserController: nismo injectovali context, IUserRepository

Relations:
Moramo povezati tabele sami, da bi dobili povezane podatke iz baze (photos...)
Kada to uradimo, dobijamo gresku JSON serializer, to resavamo tako sto pravimo DTO, i stavljamo unutra sve sto zelimo da vratimo kada se pozove metoda lsitOfUsers.Kada smo u kontroleru Users, i zelimo listu usera, kada se vrati sa servera, konvertuje se u AppUser, a mi zelimo da AppUsera mapiramo na MemberDto, iz tog razloga koristimo AutoMapper, mogli smo i da kad vracamo AppUsera da vracamo MemberDto, i u return da napisemo nesto ovako: return new memberDto { pa da mapiramo svaki properti rucno}
Pravimo foleder Helpers i u njemu AutoMapperProfile, u kontruktoru pravimo mapu 
U kontroleru iz metode vracamo MemberDto i pravimo usersToReturn, sve se lepo mapiralo sem photoUrl, jer zelimo da se u useru prikaze u propertiju photourl, ona fotka koja je main u photo entitetu to resavamo ovako:
.ForMember(d => d.PhotoUrl, o => o.MapFrom(s => s.Photos.FirstOrDefault(x => x.IsMain)!.Url));
Na kraju, pravimo kod koji je clean, u userRepository iz baze ne vracamo AppUsera nego odmah MemberDto i radimo sa njim,
jedino sto dodajemo je Queriable Extension, to znaci da ne moramo da mapiramo unutar kontrolera, vec u repositoryPattern pomocu queryable extensions koristimo projekciju iz repozitorijuma u dto

(Postman)
Kad hocemo da vratimo sve usere, moramo da se prijavimo kao neko ko je vec registrovan, i da iskoristimo njegov token u Authentication zagalvlju

Sada zelimo da ono sto server vraca, napravimo takav interface i da dobijemo podatke na klijentu, imamo tool koji radi to (pretvara json format u interface), zove se jsontots

Zasto sam stavio environments.ts umesto .development, zato sto pise u angular.json da su zamenjeni i da ako koristimo environments.ts, zapravo koristimo .development

Input elementi sluze da primaju od roditelja, ali to su zapravo i signali, tako da mogu da se koriste kao signal()

Koristio sam DBeaver da vidim bazu, jer u sqlite u vs code ne moze da se edituje (to je u sustini database tool)

Da bi se dobila lista svih usera, korisnik mora biti autentifikovan i to je jasno, mi smo do sada slali u http zahtev options i tu Bearer token, to je dobro ako imamo malo zahteva ali ako imamo vise, pravimo interceptor, sto znaci da nam ne treba options zaglavlje u memberService, a to znaci da ukoliko smo autentifikovani, onda svaki req ce imati authorization header.Moramo u appConfig da registrujemo taj interceptor
Interceptori zapravo sluze tako sto se svaki put pozovu kada napravimo http zahtev, kao za loading spiner, u interceptor smo napravili da kada se posalje http zahtev da se loading spiner setuje, kada se dobije response, onda se ugasi
Korisitmo ngx-spinner za cekanje nekih stavki, treba da stavimo loading spinner komponentu sto vise u nasoj apk, pravimo interceptor za loading spiner i servis
Sada imamo problem da imamo previse loading spinera za svaki http zahtev, zelimo da imamo loading spiner kada se nomralno ucita apk privi put, ali posle toga da kada se predje sa kartice listOFUsers na kokretnog usera, zelimo da se to negde cuva i to resavamo pomocu signala
U memberService postavljamo signal listu membera i subscrajbujemo se tu u servisu i postavimo signal (set), resili smo problem sa prelaska izmedju komponenti Matches i list, sada zelimo sve to isto da uradimo samo nad komponentom konkretnog usera, to resavamo malo drugacije u servisu.I na kraju naravno to zelimo da uradimo u updateUser 

Koristio sam ng-gallery za slike

Kada koristimo formsModule, jedino dugme unutar forme ce raditi ako je tipa submit, ako zelimo neko drugo dugme da bude submit, nasoj formi moramo dodati id, a dugmetu form="isto ime kao id"

Dekorator ViewChild omogucava pristup template reference elementima koje smo dodelili formi, da mozemo pristupati njima iz .ts fajla

Zelimo da kad korisnik edituje profil i ode na neku rutu, ona ga upozorava da prethodono sacuva promene, inace nece biti izmena sacuvana
zato pravimo jos jedan guard (can Deactivate)

Ukoliko korisnik napusti stranicu, ima opcija da takodje vodi racuna jer promene nece biti sacuvane, to se ovako radi: @HostListener('window:beforeunload', ['$event']) notify($event: any) {
    if (this.editForm?.dirty) {
      $event.returnValue = true;
    }
  }


Sada zelimo obrnutu situaciju, da mapiramo MemberUpdateDto u AppUser, kada updatujemo usera

Za smestanje slika koristicemo servis koji se zove Cloudinary, imamo 10GB storage-a for free za koriscenje
Kako ce ici procedura:
1.klijet uplouduje sliku sa jwt tokenom (samo autentifikovan korisnik ce moci da uploaduje sliku)
2.Server uplouduje sliku na Cloudinary(ima API kljuc koji mozemo koristiti tako da samo nas server moze pristupiti cloudinary)
3.cloudinary smesta sliku i vraca response (response sadrzi publicId i url)
4.API cuva url slike i public ID iz Cloudinary-a
5.DB vraca serveru autogenerisani ID
6. 201 Created Response- to je ono sto vracamo korsiniku sa location header

Gde se taj kljuc nalazi? (korak 3)
Programabile data => view API keys

Za front koristimo ng2-file-upload package i angular file upload valor software,
Svuda u html koji sam kopirao sa neta, gde ima greske dodao sam ?

Kada uploadujemo sliku, na serveru radi, na frontu radi, ali kad odemo na tab matches, i dalje stoji stara slika dok se ne refresuje stranica, to je problem u member servisu, treba signal da se update-uje:
U f-ju setMainPhoto ne prosledjujemo vise photoId: number, vec photo:Photo

Trenutno sam koristio template forms, tako sto u html kodu dodam template reference i pokupim podatke kad se klikne submit, sada cemo korsititi reaktivne forme, to prvo radimo u register komponenti (Vise ne koristimo FromsMoudule vec ReactiveFormsModule)
pravimo reaktivnu formu FormGroup i propertije unutar FormControl, u html kodu umesto #imeForme, koristimo [formGroup].
Umesto [(ngModel)]="model.username", koristimo formControlName="username"
Za confirm password treba nam customValidator, koji se pise ovako: 
matchValues(matchTo: string): ValidatorFn {
    return (control: AbstractControl) => {
      return control.value === control.parent?.get(matchTo)?.value ? null : {isMatching: true}
    }
  }
Ali ako korisnik zeli da promeni password, u confirmPassword ce i dalje pisati valid, to resavamo ovako: 
 this.registerForm.controls['password'].valueChanges.subscribe({
      next: () => this.registerForm.controls['confirmPassword'].updateValueAndValidity()
    })

Koristimo bootstrap, forms => validations da korisniku prikazemo da li je valid ili ne
Jos jedna napredna stavka, umest formGroup, injectujemo servis i korisitimo formBuilder!

za dateTime mozemo jednostavno u html da stavimo type="'date'", ali ja cu koristiti angular bootstrap date picker
Kad se registrujemo na klijentu u sekciji datetime, kad selektujemo datum, on ga pamti kao : 2025-02-11T15:30:49.000Z, a nama treba dateOnly,to resavamo ovako: 
 private getDateOnly(dob: string | undefined) {
    if (!dob) return;
    return new Date(dob).toISOString().slice(0,10);
  }

Sta je paginacija???
Ako imamo mnogo korisnika, ne treba mnogo stvari da vracamo iz baze zbog performansi
IQueryable<Uer>	==> var query = context.Users
					.Where(x => x.Gender == gender)
					.OrderBy(x => x.Username)
					.Take(5)
					.Skip(5)
					.AsQuerable()

posle to moramo pozvati query.ToListAsync(), query.ToDictionary()..... da bi se videle promene u bazi

Pagination Header koristimo jer zelimo da vratimo PagedList u header, moze i kroz body, svejdeno


Kreiramo tabelu spoja UserLike jer zelimo tako da se zove, kada smo dodali u UserEntity vise na vise vezu sa samim AppUserom, i kreirali migracije, on nam je napravio sam tabelu AppUserAppUser, sto mi ne zelimo tako da se zove nego je sami pravimo 
Kada to radimo, mora da kazemo EF sta koristimo za primary key a i veze

Kada odredjeni koristnik ima neku interakciju na profilu(pritom je logovan), zelimo da updatujemo LastActive properti u njegovom profilu, iz tog razloga koristimo action filter
Dodajemo u helpers LogUserActivity
Korisitmo interfejs IAsyncActionFilter
Registrujemo servis
Dodamo ovo u baseApiController : [ServiceFilter(typeof(LogUserActivity))]

Sada zelimo na klijentu da prikazemo bolje datume i vreme, tacnije (lastActive i member since)
Koristimo ngx timeago, uvezemo modul i samo dodamo | timeago i | date: dd-MMM yyyy

Kreiramo kes

==============================================================
163 i 164 i 175 nisam odgledao! - to je kesiranje
==============================================================
i 175 likesa feature, odgledaj kad odgledas ovo iznad

AutoMapper nam zapravo sluci da kreiramo mapu kako ce se recimo message mapirati na messageDto, isti propertiji se mapiraju automatski, a pojedine propertije mi definisemo kako ce se mapirati!

Zelimo da loading bude tek kada se udje u messages tab u profilu, problem je jer unutar memberDetail komponente koristimo child komponentu koja ima ngOnInit i zato i loading imamo.
Dodajemo #memberTbas (ime tabseta), a ovo dodajemo u .ts fajl
@ViewChild('memberTabs') memberTabs?: TabsetComponent; (da bi videli u .ts fajlu ono sto je u htmlu)
Ono sto jos trebamo uraditi jeste da u memerDetail komponenti ucitamo poruke i da ih prosledimo u child komponentu (memberessages)


Zelimo da kada kliknemo na messages- da se otvori u profilu tab messages
 <button (click)="selectTab('Messages')" class="btn btn-success">Message</button> ovo je heading ('Messages')

Zelimo sada kad kliknemo na Matches kartici na posalji poruku, da nas odvede na taj profil 
To postizemo sa queryParams:
[queryParams]="{tab: 'Messages'}"> 

==============================================================================================================================================
190 nisam odgledao - korisi se route Resolves (zeli da kad kiknemo na posalji poruku iz Matches da nas odvede na profile i na karticu Message)
==============================================================================================================================================

(click)="$event.stopPropagation()" znaci da se ne odradi rutiranje vec samo izvrsi clik koji je nakon ovoga, u nasem slucaju delete















